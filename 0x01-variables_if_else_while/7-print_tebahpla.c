#include <stdio.h>

/**
 *main - Entry point
 *
 * Description: 'Decrementing the alphabet loop  '
 *
 * Return: Always 0 (Success)
 */

int main(void)

{
	char z

	char = 'z'

	while (i >= 'a')
	{putchar(z);
	i--;
	}
	putchar('\n');
	return (0);
}
