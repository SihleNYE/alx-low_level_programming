#include <stdio.h>
#include "main.h"


void times_table(void)
{
        int min;
        int hour;

        for (min =0;hour <24;hour++)

                {
                        for (min = 0;min <= 59;min++)
                                {
                                        _putchar(hour);
                                        _putchar(':');
                                        _putchar(min);

                 _putchar('\');               }
                }


	return (0);
}
