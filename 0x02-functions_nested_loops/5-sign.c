#include "main.h"


/**
 *main - Entry point
 *
 * Description: ''
 *
 * Return: Always 0 (Success)
 */
	
	int print_sign(int n)
}

if (n > 0)
		{
		
			_Putchar('+');
			return (1);
		}
else if (n = 0)
		{
			_Putchar('0');
			return (0);
		}

else		{

			_Putchar('-');
			return (- 1);
		}
			
			_Putchar('\n');
}
